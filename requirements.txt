# Nothing is required to read the skills. These are for the bundled scripts.

# REST calls and OneLake DFS listing
httpx>=0.27

# Delta read/write via delta-rs.
# NOTE: in deltalake 1.x, get_add_actions() returns an arro3 table, not a
# PyArrow one — convert with pa.table(...) before use. See
# skills/connecting-to-fabric/reference/onelake.md
deltalake>=1.0
pyarrow>=16

# SQL over TDS. PREFER go-sqlcmd instead: a single static binary with built-in
# Entra service-principal auth, no ODBC driver and no admin rights.
#   https://github.com/microsoft/go-sqlcmd
#
# pyodbc additionally needs the system driver "ODBC Driver 18 for SQL Server":
#   Windows       msodbcsql.msi (requires admin)
#                 https://learn.microsoft.com/sql/connect/odbc/download-odbc-driver-for-sql-server
#   Debian/Ubuntu sudo apt-get install -y msodbcsql18 unixodbc
#   RHEL/Fedora   sudo yum install -y msodbcsql18 unixODBC
pyodbc>=5.1
